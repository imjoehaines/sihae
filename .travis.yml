language: php

php:
  - 5.5
  - 5.6
  - 7.0

before_script:
  - psql -c 'create database sihae;' -U postgres
  - composer self-update
  - composer install
  - npm install
  - cp .env.travis .env
  - php artisan migrate

script:
  - npm test

matrix:
  allow_failures:
    - php: 7.0
