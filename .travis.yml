language: php

php:
  - 7.0
  - 7.1
  - nightly

before_script:
  - psql -c 'create database sihae;' -U postgres
  - composer install
  - cp .env.travis .env
  - php artisan migrate

script:
  - npm test

matrix:
  allow_failures:
    - php: 7.1
    - php: nightly
